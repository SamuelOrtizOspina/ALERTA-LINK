# =============================================================================
# ALERTA-LINK - Variables de Entorno (PLANTILLA)
# =============================================================================
# INSTRUCCIONES:
# 1. Copia este archivo como .env: cp .env.example .env
# 2. Completa TODAS las variables marcadas como OBLIGATORIO
# 3. NUNCA subas el archivo .env a git
# =============================================================================

# -----------------------------------------------------------------------------
# Configuracion General
# -----------------------------------------------------------------------------
APP_NAME=ALERTA-LINK
APP_VERSION=0.1.0
# DEBUG=true solo para desarrollo local, NUNCA en produccion
DEBUG=false

# -----------------------------------------------------------------------------
# Base de Datos (OPCIONAL)
# -----------------------------------------------------------------------------
# Si no se configura, usa archivos JSONL como fallback
# Formato: postgresql://usuario:password@host:puerto/nombre_db
DATABASE_URL=postgresql://usuario:password_seguro@localhost:5432/alertalink

# -----------------------------------------------------------------------------
# Seguridad (OBLIGATORIO en produccion)
# -----------------------------------------------------------------------------
# Generar con: python -c "import secrets; print(secrets.token_urlsafe(32))"
SECRET_KEY=CAMBIAR_POR_CLAVE_SEGURA_GENERADA

# -----------------------------------------------------------------------------
# API Keys Externas
# -----------------------------------------------------------------------------

# Tranco API - Lista de dominios legitimos (Top 1M)
# Registro gratuito en: https://tranco-list.eu/
TRANCO_API_KEY=tu-api-key-de-tranco
TRANCO_API_EMAIL=tu-email@ejemplo.com
TRANCO_RANK_THRESHOLD=100000

# VirusTotal API - Verificacion de URLs maliciosas
# Registro gratuito en: https://www.virustotal.com/gui/my-apikey
# Limite API gratuita: 4 requests/minuto, 500 requests/dia
VIRUSTOTAL_API_KEY=tu-api-key-de-virustotal
VIRUSTOTAL_THRESHOLD=3
VIRUSTOTAL_UNCERTAINTY_MIN=30
VIRUSTOTAL_UNCERTAINTY_MAX=70

# -----------------------------------------------------------------------------
# CORS (Cross-Origin Resource Sharing)
# -----------------------------------------------------------------------------
# Lista de dominios permitidos separados por coma
# NUNCA usar "*" en produccion
CORS_ORIGINS=https://samuelortizospina.me,https://api.samuelortizospina.me,http://localhost:8000

# -----------------------------------------------------------------------------
# NOTAS DE SEGURIDAD
# -----------------------------------------------------------------------------
# 1. Este archivo (.env) debe estar en .gitignore
# 2. Generar SECRET_KEY unica para cada ambiente
# 3. Usar HTTPS en produccion
# 4. No usar DEBUG=true en produccion
# =============================================================================
