<?xml version="1.0" encoding="utf-8"?>
<!--
    ALERTA-LINK - Configuracion de Seguridad de Red

    PRODUCCION: Solo permite trafico HTTPS cifrado
    DEBUG: Permite HTTP solo para localhost/emulador
-->
<network-security-config>
    <!-- Configuracion base: Solo HTTPS -->
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>

    <!-- Excepcion para desarrollo local (emulador Android) -->
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="true">10.0.2.2</domain>
        <domain includeSubdomains="true">localhost</domain>
        <domain includeSubdomains="true">127.0.0.1</domain>
    </domain-config>

    <!-- Dominio de produccion - solo HTTPS -->
    <domain-config cleartextTrafficPermitted="false">
        <domain includeSubdomains="true">samuelortizospina.me</domain>
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </domain-config>
</network-security-config>
